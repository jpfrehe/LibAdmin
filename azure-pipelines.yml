# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: DockerCompose@0
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: 'Azure for Students(1)(7a060baa-44e3-45d1-9a64-707288ea1730)'
    azureContainerRegistry: '{"loginServer":"sacregistry.azurecr.io", "id" : "/subscriptions/7a060baa-44e3-45d1-9a64-707288ea1730/resourceGroups/SAC1/providers/Microsoft.ContainerRegistry/registries/sacRegistry"}'
    dockerComposeFile: '**/docker-compose.yml'
    action: 'Run a Docker Compose command'
    dockerComposeCommand: 'docker-compose up'
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
